java 8 install
--------------

$ sudo apt-get update
$ sudo apt-get install openjdk-8-jdk

If you have two versions of java you can choose which version you want using below command

$ sudo update-alternatives --config java 
